---
ansible-navigator:
  ansible:
    inventory:
      entries:
      - ./inventory 

  execution-environment:
    image: registry.redhat.io/ansible-automation-platform-24/ee-minimal-rhel9
    enabled: true
    container-engine: podman
    pull:
      policy: missing

